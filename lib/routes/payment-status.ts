import type {VercelRequest,VercelResponse} from '@vercel/node';import {json,method,requireApi,sql} from '../db';export default async function handler(req:VercelRequest,res:VercelResponse){if(!method(req,res,['GET']))return;const key=requireApi(req);if(!key)return json(res,401,{success:false,error:'X-API-Key wajib'});const id=String(req.query.transaction_id||'');const r=await sql`SELECT t.id,t.order_id,t.amount,t.status,t.expires_at,t.created_at FROM transactions t JOIN merchants m ON m.id=t.merchant_id WHERE t.id=${id} AND m.api_key=${key} LIMIT 1`;if(!r.rowCount)return json(res,404,{success:false,error:'Transaksi tidak ditemukan'});const x=r.rows[0];let status=x.status;if(status==='pending'&&new Date(x.expires_at).getTime()<Date.now())status='expired';json(res,200,{success:true,transaction:{...x,status}})}
