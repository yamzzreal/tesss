import type {VercelRequest,VercelResponse} from '@vercel/node';import {compare,json,method,sessionCookie,hash} from '../db';export default async function handler(req:VercelRequest,res:VercelResponse){if(!method(req,res,['POST']))return;const b=req.body||{},email=String(b.email||'').trim().toLowerCase(),password=String(b.password||'');const expectedEmail=String(process.env.ADMIN_EMAIL||'').trim().toLowerCase(),stored=String(process.env.ADMIN_PASSWORD||'');if(!expectedEmail||!stored||email!==expectedEmail||password!==stored)return json(res,401,{success:false,error:'Admin login salah'});res.setHeader('Set-Cookie',sessionCookie('env-admin','admin'));json(res,200,{success:true})}
