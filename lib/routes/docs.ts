import type {VercelRequest,VercelResponse} from '@vercel/node';import {json,method} from '../db';export default function handler(req:VercelRequest,res:VercelResponse){if(!method(req,res,['GET']))return;json(res,200,{name:'Yamzz Payment Gateway',version:'2.0.0',model:'merchant-owned QRIS',endpoints:{register:'POST /api/register',login:'POST /api/login',saveQris:'POST /api/qris-save',products:'GET|POST|DELETE /api/products',createPayment:'POST /api/payment-create',status:'GET /api/payment-status?transaction_id=...',transactions:'GET /api/transactions'},auth:'Merchant API requests use X-API-Key. Browser dashboard uses secure session cookie.',important:'The gateway generates a nominal QR payload from the merchant static QRIS. It does not receive or hold merchant funds. Payment settlement and reliable paid notifications require a supported official transaction-status/notification channel from the merchant QRIS/acquirer.'})}
