import type {VercelRequest,VercelResponse} from '@vercel/node';import {getSession,json,method,sql} from '../db';export default async function handler(req:VercelRequest,res:VercelResponse){if(!method(req,res,['GET']))return;const s=getSession(req);if(!s)return json(res,401,{success:false,error:'Belum login'});const r=await sql`SELECT id,order_id,amount,status,expires_at,created_at FROM transactions WHERE merchant_id=${s.userId} ORDER BY created_at DESC LIMIT 100`;json(res,200,{success:true,transactions:r.rows})}
