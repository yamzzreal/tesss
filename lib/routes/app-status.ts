import type {VercelRequest,VercelResponse} from '@vercel/node';import {json,method,sql} from '../db';
export default async function handler(req:VercelRequest,res:VercelResponse){if(!method(req,res,['GET']))return;const started=Date.now();try{await sql`SELECT 1`;json(res,200,{success:true,status:'operational',database:'online',api:'online',latencyMs:Date.now()-started,time:new Date().toISOString(),version:'3.0.0'})}catch(e){json(res,503,{success:false,status:'degraded',database:'offline',api:'online',latencyMs:Date.now()-started,time:new Date().toISOString(),error:e instanceof Error?e.message:'Database unavailable'})}}
