import type {VercelRequest,VercelResponse} from '@vercel/node';import {getSession,json,method,sql} from '../lib/db';export default async function handler(req:VercelRequest,res:VercelResponse){if(!method(req,res,['GET']))return;const s=getSession(req);if(!s)return json(res,401,{success:false,error:'Belum login'});const r=await sql`SELECT id,email,name,api_key,qris_payload,qris_name,qris_city,active,created_at FROM merchants WHERE id=${s.userId}`;if(!r.rowCount)return json(res,404,{success:false,error:'Merchant tidak ditemukan'});json(res,200,{success:true,merchant:r.rows[0]})}
